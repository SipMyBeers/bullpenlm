{
  "name": "bullpenlm-email-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that sends branded outreach emails for BullpenLM hosts via the Email Service binding.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
