Drop a 30+ second audio sample of this person's voice as `sample.wav`
to enable Tier-3 voice cloning. Then run:
  python3 manage.py clone-voice acme-finance
If no clone is present, the server falls back to macOS `say -v Samantha`.
