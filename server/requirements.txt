# BullpenLM Python sidecar runtime deps.
#
# Pinned so PyInstaller builds are reproducible across the GH Actions
# release matrix and developer machines.
#
# Notes:
#   - All LLM inference goes through Ollama via HTTP (urllib in stdlib).
#     We do NOT add any LLM-client library — keep the wheel slim.
#   - chromadb is the only heavy dep. Everything else is small.
#   - postal-mime, stripe, etc. are NOT here — those routes use raw
#     urllib + the user-provided API key.
#   - Native deps (whisper, ffmpeg, cloudflared, ollama itself) are
#     bundled as separate sidecars / installed by the first-run wizard.

chromadb==1.5.9
PyYAML>=6.0
certifi>=2024.0.0

# PyInstaller is build-only — not runtime. Install via `pip install -r
# server/requirements-build.txt` for CI / release builds.
